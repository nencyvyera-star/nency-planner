# Projects

Each website lives in its own folder here. To start a new one:

1. Create a folder (e.g. `my-first-site/`)
2. Copy `../templates/starter.html` and `../templates/styles.css` into it
3. Right-click the HTML → **Open with Live Server**
